interface:
  display_name: "freetier-rotate-middleware Admin"
  short_description: "Inspect and edit routing, quotas and time-window limits for freetier-rotate-middleware v2"
  default_prompt: "Use $freetier-rotate-middleware-management to inspect gateway routing state and manage upstreams, groups and rate limits."
